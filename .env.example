# Google Drive sync (optional). Create an OAuth client ID with the setup
# wizard: ./scripts/setup-google-drive.sh — or see README "Sync" section.
# Free, client-side only; the app never sees a client secret.
VITE_GOOGLE_CLIENT_ID=
